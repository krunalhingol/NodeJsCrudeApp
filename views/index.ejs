<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <!-- Latest compiled and minified BootStrap CSS -->
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <title>My quotes app</title>
</head>
<body style="margin-left:5px">
  <p>
    This quotes app was made with NodeJs, Express, MongoDb, Ejs and some basic bootstrap.
  </p>
  <p>
    To edit a quote type the new details in the inputs and click on the edit button.
  </p>

  <ul class="quotes">
  <% for(var i=0; i<quotes.length; i++) {%>
    <li id="<%= quotes[i]._id %>">
      <span id="<%= quotes[i].quote %>"><%= quotes[i].quote %></span> -
      <span id="<%= quotes[i].name %>"><%= quotes[i].name %></span>
      <button id="update" class="btn btn-primary glyphicon glyphicon-pencil"></button>
      <button id="delete" class="btn btn-danger glyphicon glyphicon-trash"></button>
    </li>
  <% } %>
  </ul>

    <form action="/quotes" method="POST" class="form-inline">
      <input type="text" placeholder="name" name="name" id="name-input" class="form-control" required>
      <input type="text" placeholder="quote" name="quote" id="quote-input" class="form-control" required>
      <button type="submit" class="btn btn-success">Submit</button>
    </form>




<script src="main.js"></script>
</body>
</html>
